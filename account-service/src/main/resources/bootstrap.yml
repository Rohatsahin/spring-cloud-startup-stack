server:
  port: ${PORT:8990}
  use-forward-headers: false
  servlet:
    context-path: /account
    

spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://localhost:7777/config