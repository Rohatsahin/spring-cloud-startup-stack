server:
  port: ${PORT:8993}
  use-forward-headers: false
  servlet:
    context-path: /auth
    

spring:
  application:
    name: authentication-service
  cloud:
    config:
      uri: http://localhost:7777/config
  session:
   store-type: none