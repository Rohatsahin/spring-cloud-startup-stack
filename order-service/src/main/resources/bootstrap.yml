server:
  port: ${PORT:8992}
  use-forward-headers: false
  servlet:
    context-path: /order-service
    
spring:
  application:
    name: order-service
  cloud:
    refresh:
      enabled: false
    config:
      uri: http://localhost:7777/config